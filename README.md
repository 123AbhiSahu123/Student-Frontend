#) Project Start:-
npm create vite@latest my-app -- --template react-js

#) npm install react-router-dom axios react-hot-toast

#) npm install -D tailwindcss @tailwindcss/vite

#) npm install lucide-react

#) Agar tum react-router-dom use kar rahe ho, to BrowserRouter zaruri hai.

#) Outlet:- Simple rule: Jab bhi tumhare paas common layout ho (header, sidebar, footer) jo multiple pages mein same rehna chahiye, aur beech ka content hi change karna ho — tab <Outlet /> use karo parent layout component mein.

#) Web → react-dom (browser DOM)
Mobile app → react-native (iOS/Android native components)

#) User_login (Authorization) = token
#) Superadmin_login (Authorization) = adminToken